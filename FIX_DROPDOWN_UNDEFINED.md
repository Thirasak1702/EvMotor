# ?? ?????????? Dropdown Item ???? "undefined"

## ??????????
?? Requisition Items (PR), Purchase Order Items (PO), ??? Goods Receipt Items (GR) ??????????? "Add Item" ???? dropdown ?????????? "undefined - undefined" ???????????? "Code - Name"

## ??????
??? serialize JSON ?? C# ???? `System.Text.Json.JsonSerializer.Serialize` ????? **PascalCase** ???? default (Id, Code, Name) ????? JavaScript ???????????????????? **camelCase** (id, code, name)

### ????????????????? serialize
```json
[
  {
    "Id": 1,
    "Code": "ITEM001",
    "Name": "Laptop",
    "Description": "Dell Laptop",
    ...
  }
]
```

### JavaScript ?????? (????)
```javascript
itemOptions += `<option value="${item.id}">${item.code} - ${item.name}</option>`;
// item.id = undefined ?????????? property ???? "id" (????? "Id")
```

## ????????? ?

????? JavaScript ?????? **PascalCase** ????????? JSON ??? serialize ??:

### ????????????:

1. **EbikeRental.Web/Pages/Purchasing/PR/Info.cshtml**
```javascript
// ??????????
itemOptions += `<option value="${item.id}">${item.code} - ${item.name}</option>`;

// ????
itemOptions += `<option value="${item.Id}">${item.Code} - ${item.Name}</option>`;
```

2. **EbikeRental.Web/Pages/Purchasing/PO/Info.cshtml**
```javascript
// ??????????
itemOptions += `<option value="${item.id}">${item.code} - ${item.name}</option>`;

// ????
itemOptions += `<option value="${item.Id}">${item.Code} - ${item.Name}</option>`;
```

3. **EbikeRental.Web/Pages/Purchasing/GR/Info.cshtml**
```javascript
// ??????????
itemOptions += `<option value="${item.id}">${item.code} - ${item.name}</option>`;

// ????
itemOptions += `<option value="${item.Id}">${item.Code} - ${item.Name}</option>`;
```

## ???????????????? ?

??????????? "Add Item" ?????? dropdown ???????????:
```
-- Select --
ITEM001 - Laptop
ITEM002 - Mouse
ITEM003 - Keyboard
```

## ?????????

1. ???????? Purchase Requisition (PR)
2. ?????? "Add Item"
3. ???? dropdown "Item" ???????? "Code - Name" ?????? "undefined - undefined"
4. ????????????????? PO ??? GR

## ??????????????????????????

### ??????????? 1: ??? camelCase ??? serialize (?????)
```csharp
// ?? .cshtml.cs ???? Program.cs
var options = new JsonSerializerOptions
{
    PropertyNamingPolicy = JsonNamingPolicy.CamelCase
};
var json = JsonSerializer.Serialize(Model.Items, options);
```

?????? JavaScript ????? camelCase ??????:
```javascript
itemOptions += `<option value="${item.id}">${item.code} - ${item.name}</option>`;
```

### ??????????? 2: ??? PascalCase ?????? (??????????????????)
JavaScript ????????????? PascalCase ?????? C#:
```javascript
itemOptions += `<option value="${item.Id}">${item.Code} - ${item.Name}</option>`;
```

## ????

? ????????????? 3 ???? (PR, PO, GR)  
? Build ??????  
? ????? "undefined" ???????  
? Dropdown ???? Item Code ??? Name ???????

---
**?????:** ? ?????????????????  
**??????:** 2024-12-25
